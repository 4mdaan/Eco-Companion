<%- include('../partials/head', { pageCSS: 'packages' }) %>

<%- include('../partials/header', { currentPage: 'destinos' }) %>

<section class="packages-hero">
  <h1>Nossos Destinos</h1>
  <p>Descubra lugares incríveis para sua próxima aventura</p>
</section>

<main class="packages-container">
  <!-- Grid de Destinos -->
  <section class="packages-grid">
    <% destinos.forEach(function(destino) { %>
      <article class="package-card-detailed <%= destino.categoria === 'Ultra Luxury' ? 'ultra-luxury-card' : '' %>">
        <section class="package-image-large" style="background-image: url('<%= destino.imagem %>');">
          <% if (destino.categoria === 'Ultra Luxury') { %>
            <section class="package-badge ultra-luxury-badge">
              <span class="crown-icon">👑</span>
              ULTRA LUXURY
            </section>
          <% } else if (destino.destaque) { %>
            <section class="package-badge">Destaque</section>
          <% } %>
          <% if (destino.exclusividade) { %>
            <section class="exclusivity-badge">✨ <%= destino.exclusividade %></section>
          <% } %>
        </section>
        
        <section class="package-content-detailed">
          <section class="package-header">
            <h3 class="package-title-large"><%= destino.nome %></h3>
            <section class="package-location">
              <svg width="16" height="16" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"/>
              </svg>
              <%= destino.regiao %>
            </section>
          </section>
          
          <p class="package-description-detailed"><%= destino.descricao %></p>
          
          <!-- Atrações principais -->
          <section class="package-highlights">
            <% destino.atrações.slice(0, 4).forEach(function(atracao) { %>
              <section class="highlight-item">
                <svg class="highlight-icon" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
                <%= atracao %>
              </section>
            <% }); %>
          </section>
          
          
          <section class="package-pricing">
            <section class="price-info">
              <% if (destino.preco) { %>
                <section class="price-label">Experiência completa a partir de</section>
                <section class="price-value <%= destino.categoria === 'Ultra Luxury' ? 'ultra-luxury-price' : '' %>">
                  R$ <%= destino.preco ? destino.preco.toLocaleString('pt-BR') : destino.precoMinimo %>
                </section>
                <% if (destino.categoria === 'Ultra Luxury') { %>
                  <section class="price-note">*Valor por casal, 7 dias</section>
                <% } %>
              <% } else { %>
                <section class="price-label">Experiência completa a partir de</section>
                <section class="price-value">R$ <%= destino.precoMinimo || '1.200' %></section>
              <% } %>
            </section>
            <section style="margin-bottom: 1.2rem;"></section>
            <a href="/destinos/<%= destino.slug %>" class="btn-view-details">
              Ver Detalhes
            </a>
          </section>
        </section>
      </article>
    <% }); %>
  </section>
</main>

<%- include('../partials/footer') %>
<%- include('../partials/scripts') %>
