<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <meta name="description" content="<%= descricao %>">
    
    <!-- CSS Principal -->
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/components/chat-fixed.css">
    
    <!-- Font Awesome para ícones -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/imagens/favicon.ico">
</head>
<body class="chat-page">
    <%- include('../partials/header') %>
    
    <main class="chat-container">
        <!-- Cabeçalho do Chat -->
        <div class="chat-header">
            <div class="chat-header-info">
                <div class="agent-avatar">
                    <i class="fas fa-robot"></i>
                </div>
                <div class="agent-info">
                    <h2 class="agent-name">Assistente Virtual</h2>
                    <span class="agent-status online">
                        <i class="fas fa-circle"></i>
                        Online 24/7
                    </span>
                </div>
            </div>
            <div class="chat-actions">
                <button class="btn-chat-action" id="limparChat" title="Limpar conversa">
                    <i class="fas fa-trash"></i>
                </button>
                <button class="btn-chat-action" id="transferirHumano" title="Falar com humano">
                    <i class="fas fa-user"></i>
                </button>
            </div>
        </div>
        
        <!-- Área de Mensagens -->
        <div class="chat-messages" id="chatMessages">
            <div class="message bot-message welcome-message">
                <div class="message-avatar">
                    <i class="fas fa-robot"></i>
                </div>
                <div class="message-content">
                    <div class="message-bubble">
                        <p>👋 Olá! Bem-vindo ao atendimento da <strong>Eco Companion</strong>!</p>
                        <p>Sou seu assistente virtual e estou aqui para ajudar com:</p>
                        <ul>
                            <li>🌍 Informações sobre destinos</li>
                            <li>📦 Detalhes de pacotes e preços</li>
                            <li>📄 Documentação necessária</li>
                            <li>💳 Formas de pagamento</li>
                            <li>🛡️ Seguro viagem</li>
                        </ul>
                        <p>Como posso te ajudar hoje?</p>
                    </div>
                    <span class="message-time" data-time="<%= new Date().toISOString() %>"></span>
                </div>
            </div>
        </div>
        
        <!-- Indicador de digitação -->
        <div class="typing-indicator" id="typingIndicator" style="display: none;">
            <div class="message-avatar">
                <i class="fas fa-robot"></i>
            </div>
            <div class="typing-dots">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>
        
        <!-- Área de Input -->
        <div class="chat-input-container">
            <!-- Sugestões Rápidas -->
            <div class="quick-suggestions" id="quickSuggestions">
                <button class="suggestion-chip" data-message="Quais destinos vocês oferecem?">
                    🌍 Destinos
                </button>
                <button class="suggestion-chip" data-message="Quanto custa um pacote?">
                    💰 Preços
                </button>
                <button class="suggestion-chip" data-message="Que documentos preciso?">
                    📄 Documentos
                </button>
                <button class="suggestion-chip" data-message="Como funciona o pagamento?">
                    💳 Pagamento
                </button>
            </div>
            
            <!-- Input de Mensagem -->
            <div class="chat-input-wrapper">
                <div class="input-group">
                    <textarea 
                        id="messageInput" 
                        class="chat-input" 
                        placeholder="Digite sua mensagem aqui..."
                        rows="1"
                        maxlength="1000"
                    ></textarea>
                    <button id="sendButton" class="btn-send" disabled>
                        <i class="fas fa-paper-plane"></i>
                    </button>
                </div>
                <div class="input-footer">
                    <span class="char-counter" id="charCounter">0/1000</span>
                    <span class="powered-by">Powered by Eco Companion AI</span>
                </div>
            </div>
        </div>
        
        <!-- Modal de Transferência -->
        <div class="modal" id="transferModal">
            <div class="modal-content">
                <div class="modal-header">
                    <h3>Falar com Consultor Humano</h3>
                    <button class="modal-close">&times;</button>
                </div>
                <div class="modal-body">
                    <p>Você será transferido para um de nossos consultores especializados.</p>
                    <div class="form-group">
                        <label for="transferReason">Motivo da transferência:</label>
                        <select id="transferReason" class="form-select">
                            <option value="">Selecione um motivo</option>
                            <option value="cotacao">Solicitar cotação personalizada</option>
                            <option value="suporte">Problema técnico</option>
                            <option value="reclamacao">Reclamação</option>
                            <option value="outro">Outro motivo</option>
                        </select>
                    </div>
                    <div class="transfer-info">
                        <i class="fas fa-clock"></i>
                        <span>Tempo estimado de espera: 2-5 minutos</span>
                    </div>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" onclick="fecharModal()">Cancelar</button>
                    <button class="btn btn-primary" onclick="confirmarTransferencia()">Confirmar</button>
                </div>
            </div>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
    
    <!-- Scripts -->
    <script src="/js/chat-bot.js"></script>
    
    <script>
        // Configuração global do chat
        window.chatConfig = {
            apiUrl: '/chat/api',
            sessionId: 'session_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9),
            autoScroll: true,
            showTyping: true
        };
    </script>
</body>
</html>